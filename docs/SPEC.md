# 御霊おとし 仕様書

『月蝕綺譚 -Luna Occulta-』二次創作の落ち物パズルゲーム。
本書は 2026-08-29 時点の実装仕様（2026-08-19 の全面更新に、健診の修正と、わいわいSDK まわりの追加を反映）。

## 1. ゲーム概要

- ジャンル: スイカゲーム式 落ち物パズル（物理合体）
- プラットフォーム: モバイルブラウザ優先の Web（PC も可）
- 遊び方: なぞって狙い、離して御霊を落とす。同じ御霊が触れると「浄化」されて一段上の御霊に合体。壺からあふれたらゲームオーバー（＝夜明け）
- 世界観の言い回し: 公式コピーの文体に準拠（読点リズム・「夜明け＝終わり」の逆転・「もう一夜」等）
- ビジュアル: 公式トンマナ「宵闇に金 — 蒔絵の夜」の実装トークンに準拠（公式MCP `kitan-lore` の get_design_tone で取得）。宵闇藍 #131320 / 金泥 #D9A94C・#F0CE7E / 月白 #E8E4D8 / 蝕紅 #E0562F（警告と高揚のみ・面積5%以下）/ 暗紫 #8E6B9E（夜桜）

## 2. 進化の階梯（段位）

| 段 | 名前 | key | 半径(px) | 内環の色（五行） |
|---|---|---|---|---|
| 1 | ネム | nemu | 18 | 水青 #5FB4D9（甲賀・水） |
| 2 | 於兎 | oto | 24 | 土琥珀 #C08A3E（甲賀・土） |
| 3 | 餡音 | anne | 31 | 木緑 #6FB069（伊賀・木） |
| 4 | ネコマタ | nekomata | 39 | 金白金 #C6BFA8（雑賀・金） |
| 5 | 弁天 | benten | 48 | 水青 #5FB4D9（雑賀・水） |
| 6 | 宇迦 | uka | 58 | 火紅 #E0562F（甲賀・火） |
| 7 | イズナ | izuna | 70 | 金白金 #C6BFA8（甲賀・金） |
| 8 | 紫苑 | shion | 84 | 木緑 #6FB069（伊賀・木） |
| 9 | 咲耶 | sakuya | 90 | 火紅 #E0562F（甲賀・火） |
| 10 | 満月 | (絵はコード描画) | 106 | 金泥 #F0CE7E |

- 五行はMCPの公式設定で裏取り（「五行色は属性表現のみに使う」の公式ルールに準拠した使い方）
- 出現抽選: 1〜5段のみ。重み 30/25/20/15/10
- 合体スコア: 生成された段の点 `[1,3,6,10,15,21,28,36,45,55]`
- **月光の浄化**: 満月誕生時、盤面の 1〜3 段の御霊を最大4体自動浄化（各段位分を加点）
- **皆既月蝕**: 満月×2 が接触 → 触れ合ったまま演出（下記）→ 消滅して +100

## 3. 連鎖（因果ベース）

- 合体で生まれた玉は「親の連鎖深度+1」を継承し、生まれて **1.6秒（CHAIN_HOT_MS）** の間だけ「熱い」状態
- 熱い玉が関与した合体だけ連鎖が伸びる。プレイヤーが落とした玉は深度0なので、**タップ連打の同時多発浄化は連鎖にならない**（純粋なカスケードのみ）
- 深度2以上で盤面右肩に縦書き金文字「二連浄化」〜「十連浄化」（掛け軸ふうに現れて消える）＋金の粒が増える
- 琴の音が連鎖のたびに陰旋法のまま音階を駆け上がる（+2段/連鎖・上限+8、3連鎖以降は3音目のきらめき追加）

## 4. 盤面・物理

- 論理座標: 480×720（DPR≤2 でスケール描画、`main` 要素の実寸にフィット）
- 壺の内寸: x 46〜434（388px）。物理壁は画面上端まで延長（縁乗り防止）し、厚み80pxで貫通防止
- 物理: Matter.js 0.20。sleeping **無効**。壁摩擦 0.08、玉は restitution 0.12 / friction 0.25 / density 0.0012
- 合体判定: `collisionStart` と `collisionActive` の両方。合体直後の玉は 230ms 合体しない＋誕生ポップ
- ゲームオーバー: 負け線（y=146）より上で速度 0.35 未満が 2 秒継続。落下直後 1.2 秒は猶予
- **夜明けは終端である**（2026-08-28）: `gameOver` になった時点で物理を止める（`Engine.update` を回さない）・合体判定を止める（`tryMerge` が即 return）。`endGame()` は保留中の加点（月光の浄化・皆既月蝕）を先に清算してから `score` を確定するので、**結果カード・BEST・`mitama_best` とヘッダーの数字が食い違うことはない**
- 演出中（カットイン・皆既月蝕）と図鑑オープン中は物理を完全停止

## 5. 演出

- **オープニング**: 札絵（**ランク10**・ランダム1枚）をフルスクリーンでズームアウト → 夜の帳 → 案内カード（実測 2.87 秒）。カードのタイトルは金グラデ文字＋背後に蝕環。**時間割**（2026-08-29改訂・実測）: 札絵だけを見せる `ART_HOLD_MS = 1200`ms → カードを **0% → 100% へ0.7秒で一段に**出す（出始め約1.26秒・完全不透明 約1.90秒）。**透過は通過点であって滞在地ではない**——以前は 300ms で 55% の踊り場に約2.6秒とどまり、その間だけ文字が読みの床（4.5）を割っていた（明るい札絵の上で遊び方4.09・補助2.40）。待ちの起点は `img.onload` ではなく**ページを開いた時刻**（重い端末で狙いが後ろへずれないため）。画面タップで即出すスキップと5秒の保険は据え置き。`prefers-reduced-motion` は400ms
- **満月成就**: 公式必殺カットイン（**正装・晴れ着・浴衣の3種からランダム**・アニメwebp）が黒帯上を**左→右**へスライド（絵が右向きのため前進方向）。1.75 秒停止
- **皆既月蝕**: 暗転 → 血月イラストパンチイン → **蝕環（喰われた筆円相）が血月を抱く** → 火の粉 → 金グラデ調の「皆既月蝕」大文字（e-zoom）→ シェイク。計 2.4 秒停止
- **蝕環**: 公式の視覚的署名（深紅の筆円相・約1/3欠け）。タイトルカード背後・皆既月蝕・ファビコンで一貫使用
- **月の満ち欠け**: 背景の月が最高到達段に応じて満ちる
- **環境演出**: 蒔絵調の生成背景（上縁に金雲・金花紋、両脇に金泥の細流と深紅の花びら、中央は無地の宵闇）、暗紫の桜の花びら、**闇を昇る金の塵**、ビネット
- **満月の後光**: 盤面の満月玉に脈動する金の暈（reduced-motion時は静的）
- **スコア**: M PLUS Rounded 1c、カウントアップ＋加点時ポップ。**HUDのラベルは日本語**（得点／最高／次・12px。2026-08-28に `SCORE`/`BEST`/`NEXT` から変更）
- **ゲームオーバー**: 「夜が明けた」（蝕紅）＋称号＋最高到達キャラのランク10札絵＋スコア

## 6. 御霊図鑑・称号

- **図鑑**: 下部の階梯アイコンをタップで開閉。**階梯の各段は `<button class="ladder-btn">` で、押し所は両辺24px以上・`aria-label`「◯◯の図鑑を開く」つき**（2026-08-28。公式トンマナの `hit-target` は44pxだが、10段を横一列に並べる帯なので幅390pxには入らない）（羽二重スタイルのカード・開いている間は物理停止）。ランク10札絵＋名前・よみ＋所属/五行＋公式紹介文（MCPの公開設定）。‹ › で移動
- **解禁**: 1〜5段は最初から閲覧可。6段以上は到達した段位まで（`mitama_dex_max` に永続）。未到達はシルエット＋「？？？」
- **現在地の印**（2026-08-28）: そのプレイで到達済みの段のボタンに `reached`（不透明）、最高到達段に `current`（金泥の縁）。未到達は不透明度.55で沈む。`updateHud()` から更新するので「もう一夜」で戻る
- **称号**: そのプレイの最高到達で決まる10種（宵の口→夜歩きの見習い→夜歩きの手習い→浄化の手練れ→浄夜の導き手→五行の使い手→夜番の頭→月導の行者→緋桜の同志→満月成就）。皆既月蝕を見届けたプレイは別格「蝕を見届けた者」。最高の誉れは保存され（`mitama_title`/`mitama_title_rank`）タイトル画面に「これまでの誉れ」として表示

## 7. 音

- BGM: 公式メインテーマ AAC 80kbps。音量 0.16。ループ
- **琴（浄化・満月・皆既月蝕）**: 実サンプル再生。AI生成の琴独奏曲（Lovart）から切り出したワンショット2種（`koto_pluck_main.m4a`=G3単音 196.5Hz、`koto_pluck_high.m4a`=高音装飾句・固定ピッチ1.12倍で再生）。単音を playbackRate 変速で陰旋法（都節）に展開、step 11（932Hz）上限でオクターブ折り返し。毎回音量±8%・ピッチ±0.4%の揺らぎ、変速率2.5超は下オクターブを40%で重ねる。装飾句は3連鎖以上と満月成就で鳴る。デコード完了までは Karplus-Strong 合成でフォールバック
- 拍子木・鈴・太鼓は WebAudio 合成のまま（拍子木＝帯域ノイズ）。200Hz 未満の純音は使わない
- **鈴**: 盤面に満月がある間、約2.6秒おきに倍音3層の「ちりん」が薄く鳴る
- 音の入口: 「月夜に入る（音あり）」「静かに入る（音なし）」の2択（localStorage記憶）。ゲーム中は ♪ で切替
- バックグラウンド: visibilitychange で BGM 停止、復帰で再開

## 8. 入力・iOS対策

- pointermove で照準、pointerup で落下（クールダウン 550ms）
- iOS Safari のダブルタップズームを JS で抑止（350ms 以内連続タップの2回目を preventDefault・ボタン除外）
- ピンチは `gesturestart` を preventDefault するが、**`visualViewport.scale` が等倍（≦1.01）のときだけ**（2026-08-29）。以前は無条件だったため、一度ズームに入ると**戻すピンチまで塞いでいた**（実機で解除不能になる報告あり）。`visualViewport` が無い環境は等倍とみなす

## 9. テスト・デバッグ機能

- `#autotest`: 自動で落とし続ける（タイトル自動スキップ）
- `#test`: 開始後に咲耶ペア×2 を自動投下（**2026-08-28修正**: 以前は壺の左右端へ落としていたため2体が接触せず、満月1体で止まって皆既月蝕まで届かなかった。現在は内向きの初速をつけて必ず合体させる）。実測で 満月成就 約0.95秒 → 皆既月蝕 約8.9秒・SCORE 210（55+55+100）
- **タイトルロゴ（ヘッダーh1）3回タップ**（1.5秒以内）: 手持ちの玉が咲耶になる（撮影・デバッグ用の隠し機能）

## 9-a. わいわいSDK（セーブ保全・全国ランキング）

`https://waiwai.town/sdk.js` を1行読み込む（**`crossorigin="anonymous"` を省略しない**）。仕様の正本は https://waiwai.town/llms.txt 。

**大原則: SDKが無くても・読めなくても・応答しなくても、ゲームは一切変化なく遊べること。**
`dist/artifact.html` は Artifact の CSP で外部スクリプトが止まるため、これは想定内の常態である。
すべての呼び出しは `try/catch` と**自前のタイムアウト**で囲む（`RANK_TIMEOUT_MS` / `SAVE_TIMEOUT_MS` = 2500ms）。
SDK 自身はハンドシェイク2秒＋要求5秒待つので、素直に `await` すると結果カードが最大7秒遅れる。
**失敗は握りつぶさず `console.warn` を1行残す。**

### セーブ保全

- **わいわいタウンの枠（iframe）では、ゲーム自身の localStorage が保持されない**（2026-08-29にオーナーの iPhone で実機確認。Safari・Chrome とも、タブとアプリを終了すると最高得点が消えた。**Pages を直接開いた場合は消えない**）
- 5つの値を**1キー `mitama_save` の束**にまとめ、`waiwai.save()` / `waiwai.load()` を主経路にする（`best` / `dexMax` / `title` / `titleRank` / `sound`）
- **移行**: 初回に旧 localStorage キー（`mitama_best` ほか）を読み、あれば束にして保存してから旧キーを消す（公式の推奨手順）。**消すのは保存が解決してから**
- **`best` / `dexMax` / `titleRank` は大きいほうを採る**（`mergeSave()`）。移行と自己修復が同じ規則で片づく
- **タイムアウトと「記録が無い（`null`）」を区別する。**潰すと「読めなかった」を「初回だ」と誤読し、**空の値で向こうの記録を上書きする**。`ok:false` の夜は SDK に書かない
- チラつき対策は「**待たせる**」に寄せる。案内カードの表示自体を読み込み完了まで待たせる（0点を出してから差し替える見え方にしない）
- タウンの外では SDK が自動で localStorage 保存にフォールバックする＝**同じビルドをそのまま配布できる**
- Discordログイン中はサーバーへ自動同期され、別端末でも続きから遊べる（ゲーム側の追加実装は不要）

### 全国ランキング

- ボード名 **`main`**（既定の降順＝大きいほど上位）。**初回の送信で自動作成**され、事前登録は不要
- `endGame()` で記録を確定した直後に `submitScore("main", score, {tier, title})`。**応答を await しない**
- **順位は送信の応答に入っている**（タウン内では `{ok, best, rank, improved}` を返す）。`getMyScore` は保険。総数は `getTopScores("main",1).total`
- 結果画面に「全国◯位／◯人中」と「最高記録を更新」を出す。**取れなければ行ごと出さない**
- **受理上限（`max_score`）は設定しない**（2026-08-29 オーナー裁定）。名前の入力欄も作らない＝匿名は「ナナシ」
- 夜の通し番号 `nightId` を `restart()` で増やし、**遅れて返った前の夜の順位が次の夜のカードに出るのを塞ぐ**
- ゲームバランスを変えたらボード名を変える（`main` → `main_v2`）
- タウンの外では、SDK がブラウザ内だけの自己ベスト保持に自動フォールバックする（全国ランキングにはタウン内のプレイだけが載る）
- 結果カードは順位の2行で背が伸びるため、`@media (max-height: 700px)` で**縦の余白だけ**を詰める（横は既存の `max-width: 430px` の指定を壊さないよう、一括の `padding` で書かない）

## 9-b. 意匠の系（2026-08-28・金継ぎ健診で確定）

公式MCP `kitan-lore` の `get_design_tone` から採る。**この系に無い値を発明しない。**

- **角丸は 4 / 5 / 10px の3種**（釦5・面10・面の中の画像4）。公式は「角丸は小さめが基調: ボタン5／内側4、カード9〜10」
- **文字は12px未満を使わない**（公式の `sub-label` は9.5pxだが、それは固定解像度のゲーム内UIの値。ブラウザで読む本作は12pxを床にする）
- **押し所は両辺24px以上**（公式の `hit-target` は44px。届かない場所はその理由を書く）
- **玉の象牙色は `#FFFDF7` の1つ**（canvas の玉と、HUD・階梯のアイコンの地で同じ値を使う。以前は `#fdfbf6` と `#fffdf7` に割れていた）
- 健診の記録: `docs/ut/`（目付）／ `kitan-circle/data/kintsugi/2026-08-28/`（金継ぎ）

## 10. アセットとビルド

- 顔アイコン: 公式配布の顔アイコン(透過)（`scripts/build-faces.sh` が取得 → 段位ごとの実寸に縮小 → `src/faces.js` へ data URI 化）。玉が使う画素は最大でも r*4 なので、段位ごとにサイズを変えて重さを抑えている
- 札絵: **各キャラのランク10**（素材蔵は各キャラ 01〜10 の10段階ある。URLは台帳 index.json の url を使うこと — `fuda/`・`fuda/app/`・`fuda/v2/`・`fuda/v3/` が混在）
- カットイン: 咲耶3衣装（s5=フィニッシャー）。素材蔵には各キャラ5種・咲耶のみ15種ある
- 盤面背景: Lovart生成（ランク10札絵を参照渡し・「中央70%は無地の宵闇」制約）
- ビルド: `node scripts/build-dist.js` → index.html（単一ファイル・遅延アセット）/ dist/artifact.html（全埋め込み・約14MB）
- 動画: `scripts/preview/choreo2.js`（わいわいタウン用プレビュー動画・640×640）、`scripts/pv/`（PV制作パイプライン・720×1280、assemble.sh 参照）

## 11. 配信・掲載

- 本番: GitHub Pages https://hirohgxx.github.io/mitama-otoshi/
- 開発プレビュー: Claude Artifact（修正 → ビルド → Artifact確認 → push）
- わいわいタウン: https://waiwai.town/apps/mitama-otoshi （slug: mitama-otoshi。iframe埋め込みでサイト内プレイ。サムネ640×640・プレビュー動画15秒以内はマイページの編集から差し替え）
